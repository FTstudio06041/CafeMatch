Authlib==1.6.7
blinker==1.9.0
certifi==2026.1.4
cffi==2.0.0
charset-normalizer==3.4.4
click==8.3.1
colorama==0.4.6
cryptography==46.0.4
Flask==3.1.2
flask-cors==6.0.2
Flask-SQLAlchemy==3.1.1
greenlet==3.3.1
idna==3.11
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
pycparser==3.0
PyMySQL==1.1.2
python-dotenv==1.2.1
requests==2.32.5
SQLAlchemy==2.0.46
typing_extensions==4.15.0
urllib3==2.6.3
Werkzeug==3.1.5
Flask-Limiter==4.1.1
Flask-Migrate==4.1.0
gunicorn==21.2.0
pytest==8.2.2

# GNN 推薦（CPU 推論即可；torch 建議用 CPU 版：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu ）
torch==2.13.0
torch-geometric==2.8.0
pandas==3.0.3

# 圖片同步腳本的縮圖壓縮（scripts/sync_cafe_photos.py，缺少時會退回原檔複製）
Pillow==12.3.0

# 離題檢索（services/off_topic_rag.py）不需要額外 Python 套件，
# 但要有一顆 embedding 模型才會走語意比對：
#   ollama pull bge-m3
# 沒有的話會自動退回純 Python 的字元比對，準度較低但不會壞。
# 換別顆模型：環境變數 OFF_TOPIC_EMBED_MODEL；強制走字元比對就設成空字串。

# 服務模式的 WSGI 伺服器（scripts/run_backend_service.py）。
# 缺少時會退回 werkzeug 內建伺服器，仍可運作但不適合長時間掛著跑。
waitress==3.0.2
